# Example only. Copy to a private overlay or create the Secret out of band:
#   kubectl -n agenttwin-enterprise create secret generic enterprise-api \
#     --from-literal=MATRIX_ENTERPRISE_API_TOKEN="$TOKEN"
# Do not commit real values. This file is not applied by kustomization.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: enterprise-api
  namespace: agenttwin-enterprise
type: Opaque
stringData:
  MATRIX_ENTERPRISE_API_TOKEN: ""
