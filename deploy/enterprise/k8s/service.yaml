apiVersion: v1
kind: Service
metadata:
  name: enterprise-api
  namespace: agenttwin-enterprise
  labels:
    app.kubernetes.io/name: enterprise-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: enterprise-api
  ports:
    - name: http
      port: 8090
      targetPort: http
