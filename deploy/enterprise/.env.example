# Copy to deploy/enterprise/.env (gitignored). Never commit real values.
# The API is open in local/dev when these are unset (same as Playground).

# MATRIX_ENTERPRISE_API_TOKEN=
# MATRIX_ENTERPRISE_OIDC_DEV_SECRET=
# MATRIX_ENTERPRISE_OIDC_ISSUER=
# MATRIX_ENTERPRISE_OIDC_AUDIENCE=agenttwin-enterprise
# MATRIX_ENTERPRISE_ENV=development
# MATRIX_ENTERPRISE_REQUIRE_AUTH=0
# MATRIX_ENTERPRISE_CORS_ORIGINS=
